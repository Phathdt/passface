# Database (PostgreSQL)
# For local development, both URLs are the same
DATABASE_URL="postgresql://passface:passface_dev_password@localhost:5432/passface_dev"
DIRECT_URL="postgresql://passface:passface_dev_password@localhost:5432/passface_dev"

# For production (Vercel/Neon), use:
# DATABASE_URL="postgresql://user:pass@host-pooler.region.provider.tech/db?sslmode=require"
# DIRECT_URL="postgresql://user:pass@host.region.provider.tech/db?sslmode=require"

# WebAuthn Configuration
NEXT_PUBLIC_RP_ID="localhost"
NEXT_PUBLIC_ORIGIN="http://localhost:3000"

# Production settings (update these for deployment)
# NEXT_PUBLIC_RP_ID="yourdomain.com"
# NEXT_PUBLIC_ORIGIN="https://yourdomain.com"
